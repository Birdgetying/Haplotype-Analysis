# Star Gene Validation Progress

## 2026-06-05
- Read project instructions from AGENTS.md provided by user.
- Checked git status and found a dirty worktree with many pre-existing changes.
- Inspected core validation files and docs.
- Created task tracking files for this multi-step research and implementation task.
- Checked public rice and maize data source availability.
- Identified MaizeGo direct download links suitable for lightweight first-pass validation.
- Checked wheat article data availability and confirmed WWWG2B/Earlham/NGDC data routes.
- Ran current check-only validator for all manifest targets; all targets are blocked by missing local inputs and unresolved coordinates, as expected.
- Added standard-library tests for lightweight data metadata, CLI download printing, and marker-matrix database conversion.
- Added `star_gene_data.py` and `build_star_gene_database.py`.
- Updated manifest rice positive-control traits and data-source notes.
- Ran `python -m unittest test_star_gene_data.py -v`: passed.
- Ran `python run_star_gene_validation.py --check-only --all --no-download`: passed, all targets remain safely blocked by missing local inputs/unresolved coordinates.
- Ran `python run_star_gene_validation.py --print-downloads` for maize/rice/wheat: passed.
- Ran `python run_rice_test.py`: exit code 0; one existing rice test gene had no phenotype sample overlap and two succeeded.
- Re-ran `python -m unittest test_star_gene_data.py -v`: passed 8 tests.
- Re-ran `python run_star_gene_validation.py --check-only --all --no-download`: passed; all six manifest targets were safely blocked by missing local inputs/unresolved coordinates.
- Re-ran `python run_star_gene_validation.py --print-downloads --paper maize2019`: printed the four MaizeGo small-file download commands.
- Re-ran `python run_rice_test.py`: exit code 0; status distribution was two `success` genes and one `no_phenotype_match` gene due to no overlap between haplotype sample IDs and phenotype sample IDs.
- Re-checked MaizeGo Resources and HEAD responses for two representative small files; direct MaizeGo links remain reachable from this machine.
- User approved executing the next plan. Started Phase 6 focused on maize qHKW1/ZmBAM1d because it is the smallest and most actionable positive-control dataset.
- Inspected MaizeGo `100grainweight` values and confirmed three `-999` missing-value rows; added configurable phenotype missing-value filtering to marker-database construction.
- Scanned MaizeGo SV/pSV matrices around qHKW1 candidate windows while retaining `NN` as a valid SV state. The direct paper figure interval `chr1:30.44-30.54 Mb` had no small-resource records; the count-compatible marker `chr1_27976568_27994848_deletion_18280` matched the paper-like raw group counts (`NN=261`, `TT=169`, plus `AA=91`).
- Built `star_gene_database/maize_natgenet_2019/qHKW1` from the count-compatible marker and MaizeGo BLUP `100grainweight` after filtering `-999`; `variant_info.csv` now marks marker-name-derived deletion lengths as SVs.
- Ran `python run_star_gene_validation.py --run-analysis --paper maize2019 --target qHKW1`: qHKW1 analyzed successfully, but it did not validate scoring (`association_pvalue=0.3914`, `PVE=0.47%`, `score_r_squared=0.0008`, `score_regression_pvalue=0.5303`, low confidence).
- Final verification for this turn: `python -m unittest test_star_gene_data.py -v` passed 11 tests; `python run_star_gene_validation.py --check-only --all --no-download` passed; `python run_star_gene_validation.py --run-analysis --paper maize2019 --target qHKW1` passed with the same low-confidence negative qHKW1 result; `python run_rice_test.py` exited 0 after 234 s with status distribution `success=2`, `no_phenotype_match=1`.
- Continuation verification before commit: `python -m unittest test_star_gene_data.py -v` passed 11 tests; `python run_star_gene_validation.py --check-only --all --no-download` passed; `python run_star_gene_validation.py --run-analysis --paper maize2019 --target qHKW1` passed with low-confidence negative qHKW1 metrics (`association_pvalue=0.3914`, `PVE=0.47%`, `score_r_squared=0.0008`); `python run_rice_test.py` exited 0 in 257.70 s with status distribution `success=2`, `no_phenotype_match=1`.
- User clarified that the goal is not GS accuracy improvement, but whether haplotype scoring can identify known key genes as positive controls before applying it to unknown key-gene discovery.
- Re-checked the qHKW1 evidence and treated the prior count-compatible marker as non-diagnostic because it is not the paper Fig. 4h 8.9-kb indel.
- Tried normal direct URLs for MaizeGo `SV.386014.zip`; MaizeGo HTTP guesses returned 404, confirming that the full SV package is exposed through Baidu rather than a simple direct link.
- Added TDD coverage for MaizeGo SV candidate scanning, single-marker extraction, missing full-package reporting, and ignoring non-matrix text files.
- Added `prepare_maize2019_qhkw1_paper_genotype.py`. It refuses to build a qHKW1 database unless a strict scan of available MaizeGo/HapMap matrices finds exactly one chr1 `30.44-30.54 Mb` 8.5-9.5 kb insertion/deletion candidate.
- Ran `python prepare_maize2019_qhkw1_paper_genotype.py` against the currently available small MaizeGo matrices. It scanned 6 matrix files, found 0 paper-window 8.5-9.5 kb qHKW1 candidates, wrote `external_data/maize_natgenet_2019/maizego/qHKW1_paper_8p9kb_candidates.tsv`, and exited blocked. This is a data-availability blocker, not a method-negative result.
- Ran `python -m unittest test_star_gene_data.py -v`: passed 15 tests.
- Ran `python run_star_gene_validation.py --print-downloads --paper maize2019`: printed the manual Baidu `SV.386014.zip` source and kept direct download commands limited to small HTTP MaizeGo files.
- Re-ran `python prepare_maize2019_qhkw1_paper_genotype.py`: warned that full `SV.386014.zip` is missing, scanned 6 available matrices, found 0 exact candidates, and exited blocked as designed.
- Added a manifest/validator guard so qHKW1 requires a database `gene_info.json` source of `maizego_paper_marker`; the older count-compatible substitute marker database now reports `database_source_mismatch` instead of `ready_for_analysis`.
- Re-ran `python -m unittest test_star_gene_data.py -v`: passed 17 tests.
- Re-ran `python run_star_gene_validation.py --check-only --all --no-download`: passed; qHKW1 now reports `unsupported_input_format_for_analysis` with `database_source_mismatch:required=maizego_paper_marker,actual=marker_matrix`.
- Re-ran `python prepare_maize2019_qhkw1_paper_genotype.py`: same expected blocked state because `SV.386014.zip` is not present and the available 6 matrices contain 0 exact paper-window candidates.
- Ran `python run_rice_test.py`: exit code 0 in 270.87 s; status distribution `success=2`, `no_phenotype_match=1`.
- User downloaded `SV.386014.zip`; confirmed it exists at `external_data/maize_natgenet_2019/maizego/SV.386014.zip` with size `107,394,066` bytes.
- Ran `python prepare_maize2019_qhkw1_paper_genotype.py --force-extract`: it found 6 candidate matrix files, 2 full-package SV catalogue files, 0 sample-level paper-window qHKW1 8.5-9.5 kb candidates, and 0 catalogue candidates in chr1 `30.44-30.54 Mb`; exit code was blocked as expected.
- Inspected extracted full-package files `svs.final.ms.txt` and `svs.final.bs.txt`; they are no-header SV catalogue tables, not accession-by-marker genotype matrices.
- Added TDD coverage and implementation for scanning no-sample MaizeGo SV catalogue files and writing `qHKW1_paper_8p9kb_catalogue_candidates.tsv` as a diagnostic output while still refusing to build a qHKW1 database from catalogue-only records.
- Ran an expanded diagnostic scan over chr1 `27-31 Mb`, SV length 8.5-9.5 kb: found 8 MS catalogue records and 0 BS catalogue records; chr1-wide 8.5-9.5 kb counts were 269 MS and 258 BS. These remain non-actionable for haplotype scoring because sample genotypes are absent.
- Ran targeted new tests for catalogue scanning and no-reextract behavior; all passed.
- Ran `python -m unittest test_star_gene_data.py -v`: passed 20 tests.
- Ran `python prepare_maize2019_qhkw1_paper_genotype.py`: exited blocked with 6 candidate matrix files, 2 no-sample catalogue files, 0 matrix candidates, and 0 paper-window catalogue candidates.
- Ran `python run_star_gene_validation.py --check-only --all --no-download`: passed; qHKW1 remains blocked by `database_source_mismatch`.
- Ran `python run_rice_test.py`: exit code 0 in 181.53 s; status distribution `success=2`, `no_phenotype_match=1`.
- Ran `python run_star_gene_validation.py --run-analysis --paper maize2019 --target qHKW1`: qHKW1 did not run and remains `unsupported_input_format_for_analysis` because the local database source is the old substitute `marker_matrix`.
- Ran `git diff --check` on this turn's files: no whitespace errors; only CRLF conversion warnings from git.
- Investigated how to obtain the exact qHKW1 Fig. 4h 8.9-kb indel per-accession genotype table. Confirmed Nature supplementary files MOESM1-6 do not provide a Fig. 4h source-data table.
- Downloaded MOESM2, MOESM4, and MOESM5 for completeness; none contains qHKW1 per-accession genotype data.
- Rechecked MaizeGo Resources: contact email is `yjianbing@mail.hzau.edu.cn`, and the page explicitly says the group can share data for specific gene or region analysis.
- Extracted public qHKW1-nearby 8.5-9.5 kb sample-level marker rows into ignored diagnostic files under `external_data/maize_natgenet_2019/`. The candidate `chr1_28599370_28608270_deletion_8900` has per-accession states but does not match paper Fig. 4h group sizes and is not significant with local BLUP HKW (`P=0.15997`).
- User asked to pause maize and proceed with the other two papers first.
- Rebuilt the wheat Q7B-PH Figure 3g database with `python prepare_wheat2024_q7b_ph_figure3g.py`: source rows `139`, plot-level analysis rows `139`, source `wwwg2b_q7b_ph_figure3g`, pseudo coordinate `7B:67-68`.
- Ran `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target Q7B-HT`: analysis completed. Summary metrics were `association_pvalue=0.02403055474806548`, `pve=0.29634436599693`, `score_r_squared=0.2627`, `score_regression_pvalue=1.1190721955050029e-10`, `confidence_level=high`, `direction_consistency=consistent`.
- Inspected Q7B effect table: Hap1/Par was the reference/top height haplotype (`n=62`, mean `101.161 cm`); Hap2/P effect was `-14.722 cm`; Hap3/W effect was `-8.168 cm`.
- Rechecked rice Science 2024 Figshare routes from local PowerShell. `https://api.figshare.com/v2/articles/19166475`, `https://api.figshare.com/v2/articles/19166475?private_link=12978737918eecb74903`, and `https://figshare.com/ndownloader/articles/19166475?private_link=12978737918eecb74903` all returned HTTP 403. No local `external_data/rice_science_2024` files were present.
- Ran targeted test `python -m unittest test_star_gene_data.StarGeneDataTests.test_wheat_q7b_ph_prepare_builds_figure3g_database -v`: passed.
- Ran full `python -m unittest test_star_gene_data.py -v`: initially failed because the old wheat portal test asserted zero wheat download commands after adding Q7B WWWG2B small-file downloads. Fixed the test to assert only portal/Earlham sources are instruction-only.
- Verification after the test fix: `python -m unittest test_star_gene_data.py -v` passed 22 tests; `python run_star_gene_validation.py --check-only --all --no-download` passed with Q7B-HT `ready_for_analysis_check_only`, rice targets still missing required files, and qHKW1 still blocked by source mismatch; `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target Q7B-HT` completed with the same positive-control metrics.
- User asked to inspect GW2 grain-weight genes from WheatOmics. Queried WheatOmics CGI endpoints and confirmed `TaGW2-A1 = TraesCS6A02G189300`, `TaGW2-B1 = TraesCS6B02G215300`, and `TaGW2-D1 = TraesCS6D02G176900`, with Chinese Spring 1.0 coordinates and RING-type zinc-finger/SIP5 annotation.
- Checked local wheat phenotype files. `Phe.raw2` lacks a confirmed grain-weight/TGW/HKW column, and `Phe.txt` contains `TFW_DSI`; therefore GW2 cannot be interpreted as a grain-weight validation with current local phenotype data.
- Checked local CS-IAAS GFF around the WheatOmics coordinates. The coordinate systems do not map cleanly by direct position, so a GW2 run needs either WheatOmics/IWGSC-coordinate genotype/phenotype data or a verified TraesCS-to-CSIAAS coordinate mapping before analysis.
- User reported downloads are in `D:\Desktop\data`. Inspected the actual tree: `D:\Desktop\data\GW2` contains only three WheatOmics `Sequence_download.txt` files plus one `chart.png`; there is no TaGW2 sample-level genotype matrix/VCF or grain-weight phenotype table in that folder.
- Previewed the three GW2 sequence files. They are FASTA-style genomic/gene/CDS/protein sequences for `TraesCS6A02G189300`, `TraesCS6B02G215300`, and `TraesCS6D02G176900`; `chart.png` is an expression bar chart for `TraesCS6A02G189300`. Conclusion: current GW2 downloads are metadata only and cannot be used directly for haplotype scoring.
- Also inspected `D:\Desktop\data\水稻`. It contains rice chromosome genotype tables, VCFs, H5 variant annotations, `phenos.csv`, and `phenos_modified.tsv`; phenotype columns include `Heading_date`, `Plant_height`, `Grain_weight`, `Grain_length`, and `Grain_width`. This data can support local rice positive-control runs through the existing rice scripts, but it is distinct from an already-resolved Science 2024 OsMADS22/OsFTL1 route.

## 2026-06-08
- User downloaded WatSeq chr6A and chr6D SNP VCFs plus indexes under `D:\Desktop\data\GW2`; chr6B VCF remains unavailable because the download is too slow/fails.
- Added `prepare_wheat2024_tagw2_ad.py` and manifest entries for `TaGW2-A1` and `TaGW2-D1`, with required database source `watseq_tagw2_ad_vcf` and traits `TGW_mean`, `TGW_CFLN10`, and `TGW_CFLN14`.
- Ran `python prepare_wheat2024_tagw2_ad.py`: loaded 1045 Watkins TGW phenotype samples, built `TaGW2-A1` with 55 variants and 709 samples, and built `TaGW2-D1` with 2 variants and 820 samples.
- Added/ran unit coverage for TaGW2 A/D preparation and for PCA HTML generation with only two variant features.
- Fixed PCA visualization in `haplotype_phenotype_analysis.py` so low-feature targets use `min(samples, features)` components and still generate HTML with PC2/PC3 padded to zero when needed.
- Ran `python run_star_gene_validation.py --check-only --paper wheat2024 --no-download`: Q7B-HT, TaGW2-A1, and TaGW2-D1 reported `ready_for_analysis_check_only`; RHT8 remains blocked by missing local files/coordinates.
- Ran `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target TaGW2-A1 --target TaGW2-D1`: both completed. A1 has corrected association P value `7.07e-06`, PVE `16.30%`, score `R^2=0.0431`, score P value `2.416e-08`, confidence `moderate`. D1 has corrected association P value `1.0`, PVE `3.95%`, score `R^2=0.0257`, score P value `3.893e-06`, confidence `low`.
- Generated final A/D HTML outputs: `star_gene_results/wheat_nature_2024/TaGW2-A1/TaGW2-A1.html`, `star_gene_results/wheat_nature_2024/TaGW2-A1/haplotype_score.html`, `star_gene_results/wheat_nature_2024/TaGW2-D1/TaGW2-D1.html`, and `star_gene_results/wheat_nature_2024/TaGW2-D1/haplotype_score.html`.
- Started advisor-requested Hap8 follow-up: confirm whether Hap8 is the same published functional haplotype, split Hap8 into literature SNP and background SNP components, check Watkins country differences, and record INDEL/SV status.
- Added TDD tests for TaGW2-A1 promoter-offset coordinate conversion, literature/background group classification, and binary trait summaries. The first targeted run failed as expected because `analyze_tagw2_hap8_functional_groups.py` did not exist.
- Added `analyze_tagw2_hap8_functional_groups.py`, an independent diagnosis script that leaves the core HaplotypeScorer unchanged. It maps `-739/-593` to `chr6A:237,734,096/237,734,242`, rebuilds Hap8-vs-Hap1 defining SNPs, classifies samples into `Published+Background`, `PublishedOnly`, and `Neither`, tests TGW effects, merges Watkins country origin, and writes INDEL/SV download instructions.
- Ran the targeted tests again: `python -m unittest test_star_gene_data.StarGeneDataTests.test_tagw2_hap8_maps_published_promoter_offsets_to_refseq_positions test_star_gene_data.StarGeneDataTests.test_tagw2_hap8_classifies_literature_and_background_patterns test_star_gene_data.StarGeneDataTests.test_tagw2_hap8_binary_trait_summary_reports_effect_and_pvalue -v` passed.
- Ran `python analyze_tagw2_hap8_functional_groups.py`. It found 27 Hap8-vs-Hap1 defining SNPs, 66 samples carrying the two local published promoter SNP state, 25 full Hap8 samples, and 25 background markers. The two-SNP state is distributed across `Hap7=29`, `Hap8=25`, `Hap15=8`, and `Hap17=4`, so Hap8 is not a one-to-one proxy for the published two-SNP state.
- Generated Hap8 diagnosis outputs under `star_gene_results/wheat_nature_2024/TaGW2-A1/`: `tagw2_hap8_literature_variant_coverage.csv`, `tagw2_hap8_classified_samples.csv`, `tagw2_hap8_split_group_summary.csv`, `tagw2_hap8_group_trait_tests.csv`, `tagw2_hap8_country_counts.csv`, `tagw2_hap8_country_enrichment.csv`, `tagw2_hap8_country_adjusted_trait_tests.csv`, `tagw2_hap8_haplotype_literature_status.csv`, `tagw2_hap8_indel_sv_status.csv`, `tagw2_hap8_indel_sv_download_instructions.md`, and `tagw2_hap8_functional_report.md`.
- Key Hap8 result: two published local promoter SNPs alone show a weaker TGW_mean negative effect (`-3.18 g`, `P=7.99e-04`), while the Hap8 background/full Hap8 shows a stronger negative effect (`-7.55 g`, `P=3.36e-07`), and `Published+Background` is lower than `PublishedOnly` by `-7.09 g` (`P=1.87e-05`).
- Country-origin check: the two-SNP state is strongly enriched in China (`32/66`, BH FDR `3.23e-14`), and Hap8 is mostly China (`18/25`). After within-country centering, full Hap8 remains lower for TGW_mean (`-4.74 g`, `P=1.33e-04`), so country structure is a confounder but does not fully explain the Hap8 background effect.
- Checked INDEL/SV status. No chr6A INDEL/SV VCF is present locally under `D:\Desktop\data\GW2`. Earlham chr6A INDEL VCF HEAD reports `8,519,486,608` bytes and its `.csi` reports `462,684` bytes; range HEAD returned HTTP 200 rather than 206, and local tools `pysam/bcftools/tabix` are missing. Wrote reproducible download and HPC extraction instructions to `tagw2_hap8_indel_sv_download_instructions.md`.

## 2026-06-14
- Revisited TaGW2-A1 as the current GW2 positive-control target after the Rht-A1 route proved data-limited.
- Confirmed local WSL has `bcftools 1.19` and `tabix 1.19`, so indexed VCF region queries are available from this machine.
- Diagnosed a coordinate/contig mismatch in manual checks: the local chr6A WatSeq VCF contig is `chr6A`, while the earlier suggested command used `6A`; `bcftools -r 6A:...` returns empty even for present sites.
- Verified local chr6A SNP VCF contains TaGW2-A1 promoter SNPs `SNP-988` (`chr6A:237733847 G>A`), `SNP-739` (`chr6A:237734096 G>A`), and `SNP-593` (`chr6A:237734242 A>G`) but does not contain Jaiswal `SNP-494` (`chr6A:237734341 G>A`).
- Queried the remote WheatOmics merged SNP VCF at `https://wheatomics.sdau.edu.cn/jbrowse-1.12.3-release/Chinese_Spring1.0/tracks/vcf/merge.SNP.Missing-unphasing.ID.ann.finalSID.allele2_retain.hard_retain.InbreedingCoeff_retain.clean.anno.vcf.gz`; it contains `SNP-494` and can be region-queried with `bcftools`.
- Remote WheatOmics merged SNP VCF has 1051 samples, including 827 `WATDE*` samples, so it should be usable with the Watkins TGW phenotype workbook.
- Extracted a TaGW2-A1 remote SNP micro-VCF (`192` records) to `external_data/wheat_nature_2024/tagw2_remote/TaGW2-A1.remote_wheatomics_237732651_237760058.vcf.gz`; it contains `chr6A:237734341 G>A`.
- Earlham chr6A INDEL URL currently returns a small HTML `Request Rejected` page rather than gzip data, so it is not safe to start an automated large INDEL download from this machine at this moment.
- Implemented generic literature functional-variant auditing in `star_gene_literature_audit.py`. It reads `variant_info.csv`, `haplotype_data.csv`, `haplotype_samples.csv`, and `haplotype_scores.json`, then writes `literature_variant_audit.csv/json` with marker coverage, sample segregation, top-haplotype carrier counts, and strict validation status.
- Added tests for single-marker top-haplotype matches, missing markers, expected alleles absent from current samples, monomorphic expected alleles that must not validate, and multi-marker literature haplotypes.
- Updated `star_gene_validation.py` so `--run-analysis` automatically calls the literature audit for manifest targets with `literature_variants` or `literature_haplotypes`.
- Added TaGW2-A1 Jaiswal promoter SNP metadata to `star_gene_manifest.json`: `SNP-988`, `SNP-739`, `SNP-593`, `SNP-494`, and full `Jaiswal_Hap5 = G/A/G/A`. Added a `TaGW2-A1-remoteSNP` manifest target for the already built remote WheatOmics SNP database.
- Generated ignored local audit outputs for existing TaGW2-A1 result directories. Local SNP-only `TaGW2-A1` marks `SNP-494` and full `Jaiswal_Hap5` as `missing_from_database`; remote-SNP `TaGW2-A1-remoteSNP` covers `SNP-494` but marks it `present_not_segregating` because the expected `A` allele has zero carriers among 552 complete samples.
- Started teacher-requested four-target wheat star-gene triage: Rht1, VRN, GW2, and TaPIF4.
- Confirmed exact Rht1 functional SNPs in the remote WheatOmics merged SNP VCF: `Rht-B1b` = `chr4B:30861571 C>T`, annotated `c.190C>T`, `p.Gln64*`; `Rht-D1b` = `chr4D:18781242 G>T`, annotated `c.181G>T`, `p.Glu61*`.
- Added TDD coverage and implemented `prepare_wheat2024_rht1_functional_snps.py`, which builds exact functional-SNP positive-control databases from WheatOmics merged SNP VCF and Watkins `PH_M_cm-CFLN06`. The script records blocked targets and continues when another target can be built.
- Ran `python prepare_wheat2024_rht1_functional_snps.py --min-haplotype-count 1`: `Rht-B1b` was blocked because the expected T allele is not segregating in Watkins phenotype-overlap samples; `Rht-D1b` built with 802 samples and one stop-gained SNP.
- Ran `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target Rht-D1b`: generated `star_gene_results/wheat_nature_2024/Rht-D1b/Rht-D1b.html`, `haplotype_score.html`, `haplotype_scores.json`, and `literature_variant_audit.csv`.
- Rht-D1b audit result: the functional T allele has 5 carriers, the top-scored haplotype `Hap2` contains the literature allele, and the direction is lower plant height. The result is low-confidence (`R^2=0.006`, `P=0.0288`, `PVE=0.61%`) because carrier count is very small.
- Checked Watkins growth-habit status for VRN. The workbook has `GrwHabit_E_sw-CFLN06`, but no verified VRN diagnostic structural/CNV genotype table is present, so VRN is data-blocked rather than analyzed.
- Checked `QinZhen1995/CAU-TaSG` GitHub for TaPIF4. `03.PIF4_Promoter_Hap` contains scripts and `samlist`, but not final per-sample `coverage.martix`, `PIF_hap.txt`, or matching TGW/height phenotype table; TaPIF4 is not currently a clean positive control.
- Wrote `wheat_star_gene_positive_control_audit.md` summarizing which teacher targets can prove scoring effectiveness: Rht-D1b weak positive, GW2-A1 gene-level but not exact functional Hap5, VRN promising but data-blocked, TaPIF4 unsuitable without final haplotype/phenotype files.
- User approved the next step: prioritize TaGW2-B1 and VRN as stronger positive controls rather than changing the scoring algorithm first.
- Downloaded and inspected Bi et al. 2024 supplementary files for `A novel variation of TaGW2-6B increases grain weight...`; these are useful for the EMS/QTL context, but the EMS early-stop variant is not the same as the natural Watkins promoter haplotype positive-control route.
- Downloaded Qin et al. 2014 Fig. 2 image and read the TaGW2-6B natural promoter haplotype states. The favorable `Hap-6B-1` diagnostic SNPs are `-1709=A`, `-721=G`, and `-83=C`.
- Mapped TaGW2-B1 promoter offsets using the local WheatOmics sequence header for `TraesCS6B02G215300`: gene start `chr6B:291,761,229`, `CDS=169`, ATG `chr6B:291,761,397`; therefore diagnostic sites are `chr6B:291,759,688`, `chr6B:291,760,676`, and `chr6B:291,761,314`.
- Added TDD coverage for a synthetic TaGW2-B1 promoter-SNP VCF. The first run failed because `prepare_wheat2024_tagw2_b1_remote_snp.py` did not exist; after implementation the targeted test passed.
- Added `prepare_wheat2024_tagw2_b1_remote_snp.py`, which builds `TaGW2-B1-remoteSNP` only if the three literature promoter SNPs are present and segregating, and otherwise writes `external_data/wheat_nature_2024/tagw2_b1_remote/prepare_status.json` with a blocked reason.
- Added manifest target `TaGW2-B1-remoteSNP` with literature variants and `Qin2014_Hap-6B-1` haplotype pattern for downstream `literature_variant_audit.csv` generation.
- Running `python prepare_wheat2024_tagw2_b1_remote_snp.py` against the current WheatOmics remote merged SNP VCF blocked because `chr6B_291759688`, `chr6B_291760676`, and `chr6B_291761314` are missing or not segregating in that VCF source.
- HEAD checks for the guessed Earlham chr6B SNP VCF and `.csi` returned small HTML (`Content-Length: 2261`, `Content-Type: text/html`) rather than VCF/index data. Local `D:\Desktop\data\GW2` still has only the chr6B `.csi` and no main `.vcf.gz`.
- Attempted to download the Kiss et al. 2014 VRN supplementary DOC as a potential independent VRN positive-control dataset; the request timed out after 120 seconds, so per-genotype VRN marker status remains uninspected.
- User clarified that slow downloads are acceptable if they can complete.
- Rechecked the Kiss et al. 2014 Springer static supplementary URL. HEAD showed the correct DOC is `8,219,648` bytes with byte-range support; the earlier local `Kiss2014_MOESM1.doc` was only `3,133,440` bytes and incomplete.
- Downloaded the complete file with a resumable `curl -L -C -` command to `external_data/literature/vrn_kiss2014/11032_2014_34_MOESM1_ESM.doc`.
- Installed lightweight parsing dependencies `olefile` and `xlrd` into ignored `external_data/_pydeps`, extracted the embedded Excel Workbook stream to `external_data/literature/vrn_kiss2014/Kiss2014_embedded_workbook.xls`, and confirmed it contains per-genotype VRN/PPD diagnostic markers and DEV49/DEV59 heading dates.
- Added TDD coverage for a synthetic Kiss workbook and implemented `prepare_wheat2024_vrn_kiss2014.py`.
- Added manifest target `VRN-Kiss2014` and a Springer download metadata entry in `star_gene_data.py`.
- Ran `python prepare_wheat2024_vrn_kiss2014.py`: built `star_gene_database/wheat_nature_2024/VRN-Kiss2014` with 676 samples, 3 VRN diagnostic markers, and 7 haplotypes.
- Ran `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target VRN-Kiss2014`: generated HTML and audit outputs under `star_gene_results/wheat_nature_2024/VRN-Kiss2014`.
- VRN result: DEV49_mean association P `1.01e-05`, PVE `4.83%`, score R² `0.0262`, score P `2.38e-05`; DEV59_mean association P `0.00168`, PVE `3.13%`, score R² `0.0147`, score P `0.00159`. Top-scored haplotype is `Hap7=1|1|1`, which contains all three VRN spring diagnostic states, but n=2 so confidence is low. The clearest stable effect is `Hap2=0|0|1` (VRN-D1 spring state), with earlier heading by `-3.36` days for DEV49 and `-2.98` days for DEV59.
- Fresh verification before committing VRN-Kiss2014 work: `python -m py_compile prepare_wheat2024_vrn_kiss2014.py star_gene_data.py star_gene_validation.py star_gene_literature_audit.py` passed; `python -m json.tool star_gene_manifest.json > $null` passed; `python -m unittest test_star_gene_data.py -v` passed 37 tests; `python run_star_gene_validation.py --check-only --paper wheat2024 --target VRN-Kiss2014 --no-download` reported `ready_for_analysis_check_only`; `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target VRN-Kiss2014` generated the expected HTML/audit outputs again. `git diff --check` on the touched files reported only CRLF conversion warnings, not whitespace errors.
- Continued after the user confirmed slow downloads are acceptable. Added `download_wwwg2b_file.py`, a resumable WWWG2B downloader that refreshes the OneDrive temporary URL, uses `curl -C -`, and moves HTML/non-gzip responses to `.invalid`.
- Used the complete downloaded WWWG2B 1047 chr6B SNP VCF at `D:\Desktop\data\GW2\chr6B.HARD.SNP.Missing-unphasing.ID.ann.finalSID.1047.allele2_retain.hard_retain.InbreedingCoeff_retain.vcf.gz` (`2,117,273,732` bytes). The earlier invalid HTML response was moved to `.invalid`.
- Rechecked TaGW2-B1 diagnostic records in the chr6B VCF. The three corresponding VCF records are `chr6B:291759689 C>A`, `chr6B:291760677 G>A`, and `chr6B:291761315 T>C`, not the initial `291759688/291760676/291761314` coordinates. Updated `prepare_wheat2024_tagw2_b1_remote_snp.py`, `star_gene_manifest.json`, and tests to use the VCF coordinates.
- Updated `prepare_wheat2024_tagw2_b1_remote_snp.py` so local indexed `.vcf.gz` files use `bcftools` region extraction through WSL on Windows, avoiding a linear scan of the 2.1 GB chr6B VCF.
- Added strict literature-audit fields `top_haplotype_exact_expected` and `exact_matching_haplotypes`. Added regression tests so a top haplotype such as `C/A|G|C` is not reported as an exact match to a published `A|G|C` haplotype.
- Ran `python prepare_wheat2024_tagw2_b1_remote_snp.py --vcf D:\Desktop\data\GW2\chr6B.HARD.SNP.Missing-unphasing.ID.ann.finalSID.1047.allele2_retain.hard_retain.InbreedingCoeff_retain.vcf.gz --min-haplotype-count 1`: built `TaGW2-B1-remoteSNP` with 816 samples, 3 variants, and 6 haplotypes.
- Ran `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target TaGW2-B1-remoteSNP`: generated HTML, score JSON, and literature audit under `star_gene_results/wheat_nature_2024/TaGW2-B1-remoteSNP`.
- TaGW2-B1 result: exact Qin2014 `Hap-6B-1 = A|G|C` is `Hap1` (n=371) and has the highest common-haplotype TGW mean (`40.970 g`); `C|G|C` is lower (`39.049 g`) and `C|G|T` is much lower (`34.997 g`). This supports the known functional direction.
- TaGW2-B1 scoring caveat: default HaplotypeScorer ranks `Hap5 = C/A|G|C` (n=7, TGW mean `39.610 g`, score `4.2581`) above exact `Hap1 = A|G|C` (score `2.5746`). The strict audit therefore marks the combined Qin2014 haplotype as `contained_in_top_haplotype_not_exact`, with exact matching haplotypes `Hap1:371`. This is evidence that the scoring/ranking rule needs refinement before unknown-gene nomination.
- Implemented `score_mode=robust_discovery` as a discovery-oriented alternative that does not use literature haplotype labels. It keeps `default` unchanged, lowers duplicate rare/special-signal weights, applies `n/(n+20)` sample reliability to unstable components, penalizes ambiguous allele tokens such as `C/A`, and writes `sample_reliability`, `ambiguity_factor`, and `haplotype_sample_count` into per-haplotype score JSON.
- Wired `--score-mode robust_discovery` through `run_star_gene_validation.py`; non-default output is written to a separate suffix directory such as `star_gene_results/wheat_nature_2024/TaGW2-B1-remoteSNP__robust_discovery`, so default results remain available for comparison.
- Ran `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target TaGW2-B1-remoteSNP --score-mode robust_discovery`. In robust mode, top rank becomes exact Qin2014 `Hap1=A|G|C` (n=371, score `1.8132`, reliability `0.9488`, ambiguity `1.0`), while rare ambiguous `Hap5=C/A|G|C` drops to third (n=7, score `0.9733`, reliability `0.2593`, ambiguity `0.7333`). The literature audit now marks `Qin2014_Hap-6B-1` as `matched_top_haplotype`.

## 2026-06-15
- User asked to proceed with checking whether other star genes' decisive literature variants had really been compared against current results.
- Verified literature/source metadata for Qin2014 TaGW2-6B, Jaiswal2015 TaGW2-A1, Peng1999/Ellis2002 Rht1, Kiss2014 VRN, and Cao2024 TaSG-D1-TaPIF4.
- Queried the Cao2024 PMC XML and confirmed the TaSG-D1 E286K mechanism plus TaPIF4 promoter `Del` and `InDel` classes; queried the `QinZhen1995/CAU-TaSG` GitHub API and confirmed `03.PIF4_Promoter_Hap` exposes scripts and `samlist`, not final `PIF_hap.txt`, `coverage.martix`, or matching phenotype tables.
- Updated `star_gene_validation_record.md` with a literature decisive-variant checklist covering TaGW2-B1, TaGW2-A1, TaGW2-D1, Rht-B1b, Rht-D1b, VRN-Kiss2014, and TaPIF4/TaSG-D1-TaPIF4.
- Updated `task_plan.md` and `findings.md` to record that TaGW2-B1 robust is the strongest current proof, Rht-D1b is weak proof, VRN is marker-level proof, and TaGW2-A1/Rht-B1b/TaPIF4 are currently data-limited or blocked.

- Started current follow-up after user approval: inspected VRN single-marker robust outputs. VRN-D1-Kiss2014__robust_discovery has top Hap2=1 (n=38), matched literature audit, DEV49 R2 0.0382 P 2.98e-07 and DEV59 R2 0.0230 P 7.49e-05; VRN-A1/B1 marker audits match but trait signals are weak. Started Rht/TaPIF4 external-data downloads with resume-capable curl; first combined download command timed out and requires partial-file inspection.

- Added and tested prepare_wheat2024_rht_zanke2014.py for Zanke et al. 2014 PLOS ONE Table S2. Built Rht-Zanke2014 with 368 samples, Rht-B1/Rht-D1 diagnostic marker panel, and multi-environment plant-height traits. Robust scoring finds strong phenotype score regression but raw top score is the high-plant-height wild type haplotype, exposing a directionality issue for decreases_trait targets; added direction-aware summary fields to report the low-phenotype directional top separately. Continued TaPIF4 Source Data resume download; official content length is 82,112,605 bytes and partial downloads time out but are resumable.
- Confirmed the current three-proof set: TaGW2-B1 robust exact Qin2014 haplotype as proof #1, VRN-D1-Kiss2014 robust single-marker diagnostic result as proof #2, and Rht-Zanke2014 direction-aware result as proof #3.
- Rebuilt TaGW2-B1 with the full complete chr6B VCF region instead of only the three Qin2014 promoter SNPs. `python prepare_wheat2024_tagw2_b1_remote_snp.py --vcf D:\Desktop\data\GW2\chr6B.HARD.SNP.Missing-unphasing.ID.ann.finalSID.1047.allele2_retain.hard_retain.InbreedingCoeff_retain.vcf.gz --min-haplotype-count 1 --max-missing-rate 0.2` produced 756 samples, 71 regional SNPs, and 112 haplotypes in `star_gene_database/wheat_nature_2024/TaGW2-B1-remoteSNP`.
- Regenerated default and robust TaGW2-B1 HTML from the full-region database. The current integrated HTML displays 23 variants, not 3; the database has 71 retained variants. Qin2014 `A/G/C` remains covered but is split by background SNPs into many full-region haplotypes, so strict full-region audit is `present_but_not_top` rather than the earlier 3-marker `matched_top_haplotype`.
- Downloaded/inspected Zanke et al. 2014 PLOS ONE Table S2 (`external_data/literature/rht_zanke2014/Table_S2_candidate_genes_phenotypes.xlsx`), which contains Rht-B1/Rht-D1 marker states and multi-environment plant heights.
- Added `prepare_wheat2024_rht_zanke2014.py`, manifest target `Rht-Zanke2014`, validation summary fields `directional_top_haplotype*`, and audit fields `directional_validation_status` plus related directional top carrier/exact-match fields.
- Ran `python prepare_wheat2024_rht_zanke2014.py`: built `star_gene_database/wheat_nature_2024/Rht-Zanke2014` with 368 samples, 2 diagnostic markers, and 4 haplotypes.
- Ran `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target Rht-Zanke2014 --score-mode robust_discovery`: raw top robust haplotype is high-plant-height `Hap2=B1a|Rht-D1a`, but stable low-phenotype directional top is `Hap1=B1a|D1b` for BLUE (n=214, mean 82.540 cm); audit marks `Rht-D1b_diagnostic_marker` as `matched_directional_top_haplotype`.
- Checked TaPIF4 Cao2024 supplementary data. `Supplementary_Data_5_accessions_haplotypes.xlsx` contains 331 accessions and TaPIF4 haplotypes, but no matching phenotype table. The official Source Data ZIP has content length 82,112,605 bytes; repeated `curl` attempts produced incomplete/corrupt ZIPs, so TaPIF4 remains blocked and was not used as a positive proof.
- Updated `star_gene_validation_record.md`, `findings.md`, and `task_plan.md` with the three-proof set, Rht directionality caveat, and TaPIF4 blocker.
- User noted the algorithm cannot only work for one gene and requested a re-check on `VRN-D1-Kiss2014` and `Rht-Zanke2014`.
- Implemented generic robust full-region fixes: position-to-sequence allele lookup in `haplotype_phenotype_analysis.py`, robust core haplotype grouping, stable support filtering for direction-aware top selection in `star_gene_validation.py`, and directional core-group audit fields in `star_gene_literature_audit.py`.
- Added regression tests for low-support direction-aware outliers and direction-aware core group selection in `test_star_gene_data.py`. The tests first failed on the existing behavior (`TinyExtreme` was selected and `pick_directional_top_core_group` did not exist), then passed after the implementation.
- Ran syntax and targeted tests: `python -m py_compile haplotype_phenotype_analysis.py star_gene_validation.py star_gene_literature_audit.py`; `python -m unittest test_star_gene_data.StarGeneDataTests.test_direction_aware_top_haplotype_uses_expected_decrease test_star_gene_data.StarGeneDataTests.test_direction_aware_top_haplotype_filters_low_support_extremes test_star_gene_data.StarGeneDataTests.test_direction_aware_top_core_group_uses_expected_direction -v`.
- Re-ran the three requested robust targets sequentially to avoid `validation_summary.csv` overwrite races:
  `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target TaGW2-B1-remoteSNP --target VRN-D1-Kiss2014 --target Rht-Zanke2014 --score-mode robust_discovery`.
- New HTML outputs are:
  `star_gene_results/wheat_nature_2024/TaGW2-B1-remoteSNP__robust_discovery/TaGW2-B1-remoteSNP.html`,
  `star_gene_results/wheat_nature_2024/VRN-D1-Kiss2014__robust_discovery/VRN-D1-Kiss2014.html`,
  and `star_gene_results/wheat_nature_2024/Rht-Zanke2014__robust_discovery/Rht-Zanke2014.html`.
- Result summary: VRN-D1-Kiss2014 remains a positive diagnostic-marker proof (`Hap2`, n=38, DEV49 `P=2.98e-07`, DEV59 `P=7.49e-05`); Rht-Zanke2014 remains a positive direction-aware proof (`Rht-D1b` matched by the BLUE directional top `Hap1`, n=214, score regression `P=1.44e-08`). Full-region TaGW2-B1 remains significant but is not a strong exact literature-haplotype proof because 71 regional SNPs split the Qin2014 promoter haplotype into background haplotypes.
- Updated `star_gene_validation_record.md`, `findings.md`, and `progress.md` with the full-region GW2 limitation and the cross-target VRN/Rht validation evidence.
- Implemented the follow-up functional sub-haplotype robust scoring pass after the full-region GW2 stress test. The key code changes are in `haplotype_phenotype_analysis.py`, `star_gene_validation.py`, and `star_gene_literature_audit.py`; tests were added in `test_star_gene_data.py`.
- Re-ran the three requested robust targets after the functional-group update:
  `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target TaGW2-B1-remoteSNP --target VRN-D1-Kiss2014 --target Rht-Zanke2014 --score-mode robust_discovery`.
- Final validation evidence now reaches the teacher's "at least 3" requirement:
  `TaGW2-B1-remoteSNP` full-region Qin2014 `Hap-6B-1` matches `directional_top_functional_group`;
  `VRN-D1-Kiss2014` `VRN-D1=1` matches top and directional top haplotype;
  `Rht-Zanke2014` `Rht-D1b` matches direction-aware top haplotype.
- Recorded the updated evidence and limitations in `star_gene_validation_record.md` and `findings.md`. Remaining caveat: TaGW2-B1 raw top functional group is not the literature group, so future reports should present raw top, direction-aware top, and functional/core groups separately instead of using raw top rank alone.
- Rechecked the 2026-06-25 teacher concern about Rht-Zanke2014. The marker partition is correct and matches the Zanke2014 panel (`Hap1=B1a|D1b` n=214, `Hap2=B1a|Rht-D1a` n=126, `Hap3=B1b|Rht-D1a` n=26, `Hap4=B1a|D1b/Rht-D1a` n=2). The confusing plot came from using raw effect-magnitude `total` for a plant-height-decreasing target.
- Added `directional_total` as the active score axis when `expected_direction` is known, while retaining raw `total` for diagnosis. Re-ran `Rht-Zanke2014__robust_discovery`; `PlantHeight_BLUE` now has top `Hap1=B1a|D1b`, `directional_total=0.9642`, `R^2=0.451`, `P=1.36e-49`; `PlantHeight_GAT_2012` also has top `Hap1`, `directional_total=0.9624`, `R^2=0.532`, `P=1.71e-60`.
- Updated `star_gene_validation_record.md` and `findings.md`; `literature_variant_audit.csv` now reports `Rht-D1b_diagnostic_marker` as both `matched_top_haplotype` and `matched_directional_top_haplotype`.
- 2026-06-26: reran the strict single-gene/base-variant Rht-D1b route. `python prepare_wheat2024_rht1_functional_snps.py --min-haplotype-count 1 --target Rht-D1b` rebuilt one exact stop-gained marker with 802 samples. `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target Rht-D1b --score-mode robust_discovery` regenerated `star_gene_results/wheat_nature_2024/Rht-D1b__robust_discovery/Rht-D1b.html` and `haplotype_score.html`.
- Rht-D1b strict result: `Hap2=T` is the top raw robust score (`n=3`, mean `84.583 cm`, score `1.0938`) and audit `Rht-D1b_stop_gained=matched_top_haplotype`; total carrier count is 5. Because support-shrunk directional top falls back to `Hap1=G`, this is kept as weak exact-SNP evidence, not a strong proof.
- Clarified in `star_gene_validation_record.md` and `findings.md` that `Rht-Zanke2014` is marker-panel reference only for strict proof because it combines `Rht-B1` and `Rht-D1`.
- Fixed `StarGeneValidator.iter_targets()` target filtering after observing that `--target Rht-D1b` also selected `Rht-Zanke2014` through alias matching. Added a unit test proving exact `target_id` requests take precedence over alias matches, then reran `Rht-D1b` analysis with `Targets selected: 1`.
- Continued the user's request to download other needed data. Added and ran `download_wheat2024_indel_microvcfs.py` for public WheatOmics INDEL VCF tracks and `download_wheat2024_indel_marker_annotations.py` for the small Zhai indel marker annotation track. Outputs are under `external_data/wheat_nature_2024/indel_microvcfs` and `external_data/wheat_nature_2024/zhai_indel_markers`.
- Checked WWWG2B and Earlham again for WatSeq INDEL/SV routes. WWWG2B `availableTable`, `fileTable`, and OneDrive URL API calls still return HTTP 526; Earlham WatSeq directory URLs return 2261-byte HTML rather than VCF data. Therefore 1051-sample WatSeq INDEL/SV/CNV files remain inaccessible from this environment.
- Current extra-data result: public INDEL VCF tracks have 0 Watkins phenotype-sample overlap; the Zhai marker track gives nearby marker annotations only for TaGW2-B1 and VRN-B1 with a 1 Mb flank, but no per-accession genotypes. These data are saved as external annotations, not used as scoring inputs.
- 2026-06-26: implemented and ran the single-marker Zanke2014 Rht-D1 clarification requested after the teacher asked not to combine multiple genes. Added `--single-marker-targets` to `prepare_wheat2024_rht_zanke2014.py`, added manifest target `Rht-D1-Zanke2014`, and added a unit test proving the single-marker database has only the `Rht-D1` column and no `|`-joined multi-marker haplotypes.
- Ran `python prepare_wheat2024_rht_zanke2014.py --single-marker-targets`: built `Rht-D1-Zanke2014` from 368 Zanke2014 Table S2 varieties.
- Ran `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target Rht-D1-Zanke2014 --score-mode robust_discovery`: generated `star_gene_results/wheat_nature_2024/Rht-D1-Zanke2014__robust_discovery/Rht-D1-Zanke2014.html`.
- Rht-D1-Zanke2014 result: `Hap1=D1b` n=214, `Hap2=Rht-D1a` n=152, `Hap3=D1b/Rht-D1a` n=2. Raw robust total top is the tiny mixed `Hap3`, but the direction-aware top is exact `Hap1=D1b`, and `literature_variant_audit.csv` reports `matched_directional_top_haplotype`.
- Browser note: attempting to navigate the in-app browser directly to the new local `file://` HTML was blocked by the browser URL policy, so the result path is recorded for manual opening instead.
- Implemented the phenotype-free `site_weighted` robust-discovery component requested after the user objected that phenotype must not participate in discovery scoring. The site weights use annotation, explicit external evidence, MAF/missingness, LD pruning, and gene-context signals only; score JSON records `site_weighting_policy.current_phenotype_used=False`.
- Added regression coverage for phenotype-free site-weight scoring, rare high-impact site retention, and snpEff `stop_gained` alias handling. The new `stop_gained` test failed before the fix because `site_weights` was empty, then passed after normalizing to internal `stop_gain`.
- Re-ran `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target TaGW2-B1-remoteSNP --target VRN-D1-Kiss2014 --target Rht-D1b --target Rht-Zanke2014 --score-mode robust_discovery`.
- Current rerun summary: `VRN-D1-Kiss2014` remains a strong marker-level proof (`Hap2`, `R^2=0.0382`, `P=2.98e-07`); strict `Rht-D1b` now includes `chr4D:18781242` as a phenotype-free `stop_gain` site weight and recovers `Hap2=T` as raw top but remains weak due to tiny carrier count; `TaGW2-B1` and `Rht-Zanke2014` remain useful mainly via direction-aware/functional-group audit rather than raw full-region top rank.
- 2026-06-27: user requested strict base-level genotype scoring and allowed long downloads/searches. Rebuilt current true-VCF targets rather than relying on marker labels:
  `python prepare_wheat2024_vrn_remote_snps.py`;
  `python prepare_wheat2024_rht1_functional_snps.py --min-haplotype-count 1 --target Rht-D1b`;
  `python prepare_wheat2024_tagw2_b1_remote_snp.py --vcf D:\Desktop\data\GW2\chr6B.HARD.SNP.Missing-unphasing.ID.ann.finalSID.1047.allele2_retain.hard_retain.InbreedingCoeff_retain.vcf.gz --min-haplotype-count 1 --max-missing-rate 0.2`.
- Ran base-level robust analyses:
  `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target VRN-A1-remoteSNP --target VRN-B1-remoteSNP --target VRN-D1-remoteSNP --score-mode robust_discovery`;
  `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target Rht-D1b --target TaGW2-B1-remoteSNP --score-mode robust_discovery`.
- Base-level rerun outputs:
  `VRN-A1-remoteSNP__robust_discovery`, `VRN-B1-remoteSNP__robust_discovery`,
  `VRN-D1-remoteSNP__robust_discovery`, `Rht-D1b__robust_discovery`, and
  `TaGW2-B1-remoteSNP__robust_discovery`.
- Base-level rerun result summary:
  `VRN-A1-remoteSNP`: 96 SNPs, 663 samples, 18 haplotypes, `R^2=0.0081`, `P=0.02045`, top `Hap4` n=39.
  `VRN-B1-remoteSNP`: 127 SNPs, 511 samples, 26 haplotypes, `R^2=0.0475`, `P=6.62e-07`, raw top `Hap8` n=10, directional top `Hap13` n=6.
  `VRN-D1-remoteSNP`: 58 SNPs, 712 samples, 21 haplotypes, `R^2=0.0028`, `P=0.1590`, raw top `Hap2` n=97.
  These VRN runs are real-base SNP regional tests, but they are SNP-only and do not contain causal VRN deletion/CNV/SV calls.
- Strict `Rht-D1b` base-level rerun: one exact stop-gained SNP `chr4D:18781242 G>T`, 802 samples, 3 haplotypes, `site_weights=1`, top `Hap2=T` n=3, total T carriers 5, score regression `R^2=0.0056`, `P=0.0338`. This remains exact but weak proof.
- `TaGW2-B1-remoteSNP` base-level rerun: 71 regional SNPs, 756 samples, 112 haplotypes, `site_weights=39`, raw top `Hap2` n=133, score regression `R^2=0.0031`, `P=0.12597`; literature audit still reports Qin2014 `Hap-6B-1` as `present_but_not_top` at full-haplotype level and `matched_directional_top_haplotype` through the direction-aware top `Hap1`.
- Retried public data channels for missing causal/SV data. Earlham WatSeq OpenData returns a 2261-byte `Request Rejected` HTML page even with browser User-Agent. WWWG2B `availableTable`/`fileTable` API calls currently return HTTP 526 invalid SSL certificate. No new sample-level INDEL/SV/CNV VCF was obtained in this pass.
- Verification after documentation update: `python -m py_compile haplotype_phenotype_analysis.py star_gene_validation.py star_gene_literature_audit.py test_star_gene_data.py` passed; `python -m unittest test_star_gene_data.py -v` passed 90 tests; `python run_star_gene_validation.py --check-only --paper wheat2024 --target Rht-D1b --target TaGW2-B1-remoteSNP --target VRN-A1-remoteSNP --target VRN-B1-remoteSNP --target VRN-D1-remoteSNP --no-download` reported all five targets ready for analysis.
- Saved the staged advisor-facing evidence matrix as `star_gene_stage_evidence_matrix.csv`.
- Git note: the first commit command used `&&`, which this PowerShell version does not support; reran with `;` and created commit `0cb1ba5` (`Record base-level star gene rerun`). The local branch now reports `origin/work/star-gene-validation` at the same commit.
- Follow-up matrix verification: parsed `star_gene_stage_evidence_matrix.csv` successfully with 6 rows. All five VCF-derived targets point to existing HTML outputs; TaPIF4 is intentionally `not_run` because it lacks matched phenotype and VCF/sample genotype data.
- Follow-up tests after adding the matrix references: `python -m py_compile haplotype_phenotype_analysis.py star_gene_validation.py star_gene_literature_audit.py test_star_gene_data.py` passed; `python run_star_gene_validation.py --check-only --paper wheat2024 --target Rht-D1b --target TaGW2-B1-remoteSNP --target VRN-A1-remoteSNP --target VRN-B1-remoteSNP --target VRN-D1-remoteSNP --no-download` reported all five targets ready; `python -m unittest test_star_gene_data.py -v` passed 90 tests.
- 2026-06-27 follow-up: downloaded the correct Makhoul et al. 2022 Frontiers/PMC9676936 supplementary workbook for VRN-B1 structural variants from the PMC attachment route after solving the PMC proof-of-work cookie. The valid workbook is `external_data/literature/vrn_b1_structural/PMC9676936_Table_1.xlsx` (207,791 bytes; sheets `S1`-`S13`).
- Added `prepare_wheat2024_vrn_b1_frontiers2022.py`, manifest targets `VRN-B1-Frontiers2022` and `VRN-B1a-Frontiers2022`, and tests for building marker/phenotype tables from the Frontiers S1/S12/S13 sheets. Follow-up correction: the single-marker `VRN-B1a-Frontiers2022` target is now opt-in only and is documented as a sanity check, not discovery proof.
- Ran `python prepare_wheat2024_vrn_b1_frontiers2022.py`, then `python run_star_gene_validation.py --run-analysis --paper wheat2024 --target VRN-B1-Frontiers2022 --score-mode robust_discovery`.
- New outputs: `star_gene_results/wheat_nature_2024/VRN-B1-Frontiers2022__robust_discovery/VRN-B1-Frontiers2022.html` and `star_gene_results/wheat_nature_2024/VRN-B1a-Frontiers2022__robust_discovery/VRN-B1a-Frontiers2022.html`.
- Result after applying the correct discovery-style standard: the three-marker VRN-B1 structural panel ranks `Vrn-B1f` 838 bp duplication top and marks `Vrn-B1a` as `present_but_not_top`; the literature 6,851 bp deletion ranks #2 of 3 in phenotype-free `site_weights`. The single causal-marker `VRN-B1a-Frontiers2022` target ranks `Hap2=Vrn-B1a` top, n=4, but this is now treated only as allele-coding/phenotype-direction sanity check because the literature site was selected before scoring.
